namespace MaxUndercut
{
    /// <summary>
    /// Enumerates the possible execution speeds, which correspond to the number of days of data to analyze
    /// </summary>
    public enum Speed
    {
        slow,
        fast
    }
}
